{
  "name": "@acta-products/types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types for ACTA products. Consumed from source (no build step).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@acta-products/config": "workspace:*",
    "typescript": "^5"
  }
}
